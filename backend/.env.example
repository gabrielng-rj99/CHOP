# ============================================================================
# BACKEND SECRETS CONFIGURATION
# ============================================================================
#
# IMPORTANT: This file shows REQUIRED secrets that MUST be set via
# environment variables. Never commit actual secrets to version control.
#
# Copy this file to .env (for development only) and fill in real values.
# In production, use your secret management system (Vault, AWS Secrets, etc)
#
# ============================================================================

# DATABASE SECRETS
# Required: Database password must come from environment or secret management
APP_DATABASE_PASSWORD=your-database-password-here

# JWT SECRETS
# Required: JWT secret key must be a secure random value
# Generate with: openssl rand -base64 32
APP_JWT_SECRET_KEY=your-jwt-secret-key-here

# ============================================================================
# OPTIONAL: Additional production secrets
# ============================================================================

# If using CORS with specific origins in production
# APP_SERVER_CORS_ALLOWED_ORIGINS=https://yourdomain.com

# If using SSL/TLS for database
# APP_DATABASE_SSL_MODE=require

# If using external logging service
# APP_LOGGING_ENDPOINT=https://logs.example.com

# ============================================================================
# HOW TO USE THIS FILE
# ============================================================================
#
# DEVELOPMENT (Local):
# 1. Copy: cp .env.example .env
# 2. Edit: nano .env (fill with test values)
# 3. Load: source .env && go run cmd/server/main.go
# 4. NEVER commit .env (it's in .gitignore)
#
# PRODUCTION (Docker/CI):
# 1. Use secrets management: docker secrets, GitHub Secrets, Vault, etc
# 2. Pass via environment at runtime
# 3. Example: docker run -e APP_DATABASE_PASSWORD=$SECRET ...
# 4. Example: docker run -e APP_JWT_SECRET_KEY=$SECRET ...
#
# CI/CD PIPELINE (GitHub Actions):
# 1. Store secrets in repository settings
# 2. Use in workflow:
#    - name: Deploy
#      env:
#        APP_DATABASE_PASSWORD: ${{ secrets.DB_PASSWORD }}
#        APP_JWT_SECRET_KEY: ${{ secrets.JWT_SECRET }}
#      run: ./deploy.sh
#
# ============================================================================
